version: "3"
services:
  web-content:
    image: alexsimons/gulp
    ports:
      - "3000:3000"
    volumes:
      - ./web-content:/app
    entrypoint:
      - npm
      - run
      - watch
  web-service:
    build: ./web-service
